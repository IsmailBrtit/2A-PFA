import React from 'react';
import Layout from '../../components/common/Layout';


const PartnerDashboard = ({ user }) => {
  return (
  <Layout user={user}>
      <h1>Partner Dashboard (en cours de développement)</h1>
      {/* contenu */}
    </Layout>
);
};

export default PartnerDashboard;
