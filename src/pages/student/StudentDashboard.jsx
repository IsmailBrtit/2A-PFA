import React from 'react';
import Layout from '../../components/common/Layout';


const StudentDashboard = ({ user }) => {
  return (
  <Layout user={user}>
      <h1>Student Dashboard (en cours de développement)</h1>
      {/* contenu */}
    </Layout>
);
};

export default StudentDashboard;
